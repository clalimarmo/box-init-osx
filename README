# To set up on a new machine:

	1) install xcode
	2) open xcode and agree to license
	3) run `./setup`

## To update iterm2 preferences based on current preferences:

	1) run `./extract-preferences`
	2) commit and push
